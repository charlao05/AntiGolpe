# Benchmark Fase 0A

O benchmark possui exatamente 30 casos congelados em `tests/fixtures/benchmark_cases.json`.

Os casos são fixtures e não fazem chamadas externas. O benchmark não é executado como banco de dados de produção e não deve ser alterado silenciosamente.
